## Process this file with automake to produce Makefile.in

completiondir = $(prefix)/share/bash_optparse/completion

completion_DATA = \
	bash_optparse_completion \
	README

EXTRA_DIST = $(completion_DATA)
